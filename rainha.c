#include <stdio.h>

int main() {

    //declarada a variavel com o nome da peça  do jogo
    int rainha;

    // atribuiu o valor 1 inicial para a variavel, imprime 8 vezes a direção da rainha utilizando o for, incrementa para nao ocorrer o looping
    for(rainha = 1; rainha <= 8; rainha++) {
        printf("Esquerda\n");
    }
    
    return 0;
}